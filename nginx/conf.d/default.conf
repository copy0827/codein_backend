# The official nginx image entrypoint creates a static-only default.conf when this
# file is missing. This placeholder prevents that and keeps routing in codein.conf.
server {
    listen 80;
    listen [::]:80;
    server_name _disabled_default_;
    return 444;
}
